name: Deploy to Bot Hosting

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: ✅ 배포 완료 알림
        run: echo "main 브랜치 push 감지 - 호스팅 서비스가 자동으로 재배포합니다."
